# Список задач (Project Tasks)

## Актуальные задачи:
Необходимо реализовать следующие:
Окно авторизации к БД
1) Сделать окно авторизации к БД
    - поле ввода "Имя БД"
    - поле ввода "Имя пользователя"
    - поле ввода "Порт БД"
    - поле ввода "hostname"
    - поле ввода "пароль БД"
    
Нужно сделать аналог данного кода только в отдельном окне, где пользователь сам будет вводить данные с клавиатуры:
def connect_db():
    db = QSqlDatabase.addDatabase('QPSQL')
    db.setHostName('localhost')
    db.setDatabaseName('programms')
    db.setPort(5432)
    db.setUserName('postgres')
    db.setPassword('123456')

    if not db.open():
        return False

    return True

## Важное уточнение по реализации:
Делать максимально просто! Весь SQL-код пишем прямо внутри функций кнопок (в методах класса окна), чтобы студентам было видно: "нажал кнопку — выполнился этот SQL-запрос".


